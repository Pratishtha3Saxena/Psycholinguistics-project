# Psycholinguistics-project

NOTE on sampling: items are taken as the first --max-items rows sequentially,
NOT randomly. This matches the partner's notebook (baseline_experiment_v2.ipynb)
so both pipelines operate on the same 300 sentences.